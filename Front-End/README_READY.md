# Business Front (Ready)
## Install
npm install
## Run
npm run dev

### Notes
- apiClient now sends `x-business-id` from localStorage key `current_business_id`
- AcceptInvite now stores the user in `auth_user`
